═══════════════════════════════════════════════════════════════
🚀 RENDER DEPLOYMENT - SIMPLEST METHOD (100% FREE)
═══════════════════════════════════════════════════════════════

Use your EXISTING pre-built indexes (no re-ingestion needed!)
Deploy via GitHub - let Render pull indexes from repo

TOTAL TIME: 20 minutes | COST: $0/month

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 1: Add Indexes to GitHub (5 min)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Your indexes are already built locally. Let's commit them:

cd "/Users/spartan/Documents/Data Mining/Advanced Data Mining/clinical-ai-assistant"

# Check if .gitignore blocks indexes
cat backend/.gitignore | grep index

# If indexes are ignored, temporarily allow them
git add -f backend/indexes/*.faiss backend/indexes/*.pkl

# Commit indexes
git commit -m "Add pre-built FAISS indexes for deployment"

# Push to GitHub
git push origin main

Note: GitHub allows files up to 100MB. Your largest file (diabetes_index.faiss)
might be close to this limit. If push fails, see Alternative below.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 2: Deploy Backend on Render (8 min)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Go to: https://render.com
2. Sign in with GitHub
3. Click "New +" → "Web Service"
4. Select: Clinical-Assistant-RAG
5. Click "Connect"

CONFIGURE:
  Name: clinical-ai-backend
  Region: Oregon (US West)
  Branch: main
  Root Directory: backend
  
  Runtime: Python 3
  Build Command: pip install -r requirements.txt
  Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
  
  Instance Type: Free

ADD ENVIRONMENT VARIABLE:
  Key: OPENROUTER_API_KEY
  Value: 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

6. Click "Create Web Service"
7. Wait 5-8 minutes for build & deploy
8. Once live, copy your backend URL:
   Example: https://clinical-ai-backend.onrender.com
   
9. Test: Visit https://your-backend-url.onrender.com/health
   Should show all 4 indexes loaded! ✅

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 3: Deploy Frontend on Render (5 min)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Click "New +" → "Web Service"
2. Select: Clinical-Assistant-RAG
3. Click "Connect"

CONFIGURE:
  Name: clinical-ai-frontend
  Region: Oregon
  Branch: main
  Root Directory: frontend
  
  Runtime: Node
  Build Command: npm install && npm run build
  Start Command: npm start
  
  Instance Type: Free

ADD ENVIRONMENT VARIABLE:
  Key: NEXT_PUBLIC_API_URL
  Value: [YOUR BACKEND URL FROM STEP 2]
  Example: https://clinical-ai-backend.onrender.com

4. Click "Create Web Service"
5. Wait 3-5 minutes
6. Get your live URL! 🎉

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

STEP 4: Test Your App (2 min)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Visit your frontend URL
Select "Heart Attack"
Ask: "What are the symptoms of a heart attack?"
Get AI response with sources! ✅

YOU'RE LIVE! 🚀

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ALTERNATIVE: If Git Push Fails (Large Files)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

GitHub has 100MB file limit. If your diabetes_index.faiss is too big:

OPTION 1: Use Git LFS
  brew install git-lfs
  git lfs install
  git lfs track "backend/indexes/*.faiss"
  git add .gitattributes
  git add backend/indexes/
  git commit -m "Add indexes with LFS"
  git push origin main

OPTION 2: Split Backend Deployment
  - Deploy backend code without indexes
  - Upload indexes via Render disk (needs paid Shell or SCP)
  - Cost: $7/month for Shell access

OPTION 3: Use Different Platform
  - Deploy to Railway.app instead
  - Railway might have different file size handling

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

WHY THIS METHOD IS BEST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Uses your existing indexes (already built)
✅ No new Landing AI API key needed
✅ No re-ingestion time (20+ minutes saved)
✅ No Shell access needed ($7/month saved)
✅ 100% free tier deployment
✅ Auto-deploys on git push
✅ Indexes always in sync with code

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

COST: $0/MONTH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Backend: Free ✅
Frontend: Free ✅
Total: $0/month

Limitation: Services sleep after 15 min inactivity
First request takes ~30 sec to wake up

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

READY TO START?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run this command first to commit indexes:

cd "/Users/spartan/Documents/Data Mining/Advanced Data Mining/clinical-ai-assistant"
git add -f backend/indexes/*.faiss backend/indexes/*.pkl
git commit -m "Add pre-built indexes for deployment"
git push origin main

Then go to: https://render.com 🚀

═══════════════════════════════════════════════════════════════
